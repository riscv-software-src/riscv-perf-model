on:
  pull_request:
    branches:
      - master

env:
  CACHE_VERSION: v1

name: Regress Olympia on Ubuntu
jobs:
  build_test_job:
    strategy:
      # Strategy is a matrix of debug and release builds/regression
      matrix:
        os: [ubuntu-latest]
        BUILD_TYPE: [debug, release]

    name: Ubuntu-${{ matrix.BUILD_TYPE }}
    runs-on: ${{ matrix.os }}

    # Set up a global environment variable for build scripts
    env:
      OLYMPIA_BUILD_TYPE: ${{ matrix.BUILD_TYPE }}

    steps:

      # Get Sparta
      - name: Checkout Sparta
        uses: actions/checkout@v3
        with:
          repository: sparcians/map
          path: map

      # Get Olympia
      - name: Checkout Olympia
        uses: actions/checkout@v3
        with:
          submodules: recursive

      # Setup Conda and build environment
      - name: Setup Conda Environment
        uses: actions/setup-python@v2
        with:
          python-version: 3.8

      - name: Install dependencies
        run: |
          # $CONDA is an environment variable pointing to the root of the miniconda directory
          $CONDA/bin/conda create --file  ${{ github.workspace }}/olympia/conda/environment.yml --name sparta
      #
      # Build
      #
      #- name: Build
      #  uses: ./.github/actions/build  # Uses an action.yml in directory
